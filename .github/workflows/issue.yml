name: Issue Workflow
on: issues
jobs:
  issue:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v3
      - name: Print Github Event Data
        run: echo "${{ toJson(github) }}"
